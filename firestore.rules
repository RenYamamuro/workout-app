rules_version = '2';

// ログインした本人だけが、自分のデータを読み書きできる。
// それ以外（未ログイン、他人のuid）は一切アクセスできない。
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
